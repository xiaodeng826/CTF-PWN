1 ssh是连接远端的shell(命令行解释器)的工具

2 shell执行的过程:输入了a b c，先把a b c \0读入argv字符串数组中，argc记录字符串数量。再尝试执行a命令

3 FILE结构体一个与文件交互的通道，并不是文件本身

4 fopen("文件路径", "r");    //用于初始化文件结构体
  fgets(a, b, c);      //从来源c中单次最多读取b个字符到a
  signal(a, b);    //发生a类型错误时，执行b函数
  
