1	PIE是程序本体被随机加载的权限开关，ASLR是系统环境随机加载模式的开关
	libc库是程序本体外的结构，不受PIE约束
	ASLR会随机加载链接库、栈、堆之类的结构
	//因此，如果PIE关闭，则解析工具（ghidra、gdb）分析出来的程序本体地址就是真实地址


2	函数调用规则  32位程序：从右至左依次把参数压入栈。先参数入栈，再跳转函数->参数区在返回地址的下方（栈底一端）
				 64位程序：不再使用栈来传递参数。在跳转之前会把参数弹入参数寄存器（rdi、rsi...）。
						  64位程序还有对齐规则：在要call时，要求[rsp]%16=0或者跳转时(rip指向新指令)[rsp]%16=8


3	ELF程序数据段	存放内容				是否可写
	.text			程序代码				通常只读+执行
	.rodata			常量字符串			只读
	.data			已初始化全局变量		可读写
	.bss			未初始化全局变量		可读写










12 got表是储存运行时，被引用的函数的真实地址。RELRO: No RELRO    表示got表不受保护

13 plt表单存储的是指令和操作流程信息，是一个代码表单，告诉机器要去got表单找地址
	got表单存的是函数的入口地址信息的地址，函数本体就是一堆机器码。

