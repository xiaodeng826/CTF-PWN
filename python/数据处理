一、整型->字符串
      hex(a)   //把整型a转化为16进制字符

二、字节->...
      1、字节->整型
        u32(data)        //把字节序列data转化为无符号32位整型数据
        u64(data)        //把字节序列data转化为无符号64位整型数据

      2、字节->字符
        .decode()      

三、字符串->整型
      int(data,16)      //把字符字节数据->16进制整型数据

四、字符串->字节
      .encode()          //把字符串转化字节

五、整型->字节
       byte([k])                 //把一个整数 k 转换成一个只包含 1 个字节的 bytes 数据。

三、截取
      data[start:end]      //截取索引start到end-1的数据。从0开始
      .rstrip(b'abc')      //从字符串最右边开始，把属于 a、b、c 中任意一个的字符逐个删掉，直到遇到第一个不属于这个集合的字符为止（无序但不能间隔）。
      
      
