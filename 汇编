汇编指令：
  sub a,b    ——就是把a减去b的数值
  call    ——汇编指令，会先把下一条指令的地址压入栈，然后进入对应函数
  pop rbp    ——等价于mov rbp, [rsp]，add rsp, 8。 效果：把栈顶数据弹给rbp，rsp指向的地址向栈底移动8字节（具体和架构有关）。
  leave    ——等价于mov rsp, rbp，pop rbp。  效果：让rbp,rsp分别转而存放上一个栈帧的栈底和栈顶。、
  ret    ——等价于RIP = *(RSP) ,RSP += 4。  效果：把栈顶信息作为要执行的指令的地址，栈顶指针上移。
  xor    eax, eax  ——本质上是异或，效果是让eax的数据清零
  lea rax, [rbp - 0xd0]    ——计算rbp - 0xd0地址是多少，把这个地址放入rax
  test eax, eax    ———用于判断eax里的值是正、负还是0.但是不会对值改变
  or  ... , 0  ——把...上存的内容覆写为0




寄存器：
  RSP    ——指向的是当前栈顶（也就是地址最低的那一项栈数据），通常也是最近一次压入栈的数据。
  RBP    ——存放栈底的指针，栈底则存放上一个栈帧的栈底指针
  RIP    ——存放下一条要执行的指令的地址
  rdi    ——第1个参数
  rsi    ——第2个参数
  rdx    ——第3个参数
  rcx    ——第4个参数
  r8     ——第5个参数
  r9     ——第6个参数



栈结构（普遍情况）
  高地址
+----------------+
| main返回地址    |
+----------------+
| 保存的 rbp      |
+----------------+
| canary          |
+----------------+
| local_d8[200]   |  ← 输入缓冲区
+----------------+
低地址
